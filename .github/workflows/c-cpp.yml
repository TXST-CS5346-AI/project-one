name: C/C++ CI

on:
  push:
    branches: [ bss8-patch-1 ]
  pull_request:
    branches: [ bss8-patch-1 ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: make
      run: make
